#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int a, b, x, y, z, S[3], D[2];
	cin >> a >> b >> x >> y >>z;
	S[0]=x;
	S[1]=y;
	S[2]=z;
	D[0]=a;
	D[1]=b;
	sort(S,S+3);
	sort(D,D+2);
	if (D[0]>S[0]&&D[1]>S[1])
		cout << 1 << endl;
	else 
		cout << 0 << endl;
	return 0;
}
